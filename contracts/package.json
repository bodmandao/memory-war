{
  "name": "@memory-war/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network zgTestnet"
  },
  "devDependencies": {
    "hardhat": "^2.22.17",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@memory-war/protocol-core": "0.1.0",
    "dotenv": "^16.4.7",
    "typescript": "^5.6.3",
    "ts-node": "^10.9.2"
  }
}
